from __future__ import annotations


def send_email(to_email: str, subject: str, body: str) -> None:
    # Replace with provider integration (SendGrid, Mailgun, SES).
    _ = (to_email, subject, body)
